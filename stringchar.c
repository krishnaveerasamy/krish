#include<stdio.h>
#include<string.h>
main()
{
char str[20],str2[20];
int res;
puts("enter string1:);
gets(str1);
puts("enter string2:");
get(str2);
res=strcmpi(str1,str2);
if(res==0)
puts("strings are equal");
else
puts("string are not equal");
}
