#include<stdio.h>
void main()
{
char str(1000),ch;
int i,frequency=0;
printf("enter the string");
scanf(dtr);
printf("enter the charaterto find the frequency");
scanf("%c",ch);
for(i=0;i!=0;++i)
{
if(ch==str[i])
++frequency;
}
prinf("the repeated letters are");
}
