#include<studio.h>
#include<sting.h>
void main()
{
int number;
printf("enter the number to check isomorphic number");
scanf("%d",&number);
if(number>>1)
printf(%d",& isomorphic number);
else
printf("%d,&not a iosomorphic number");
getch();
}
